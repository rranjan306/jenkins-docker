pipeline{
    agent any
    stages {
        stage('Build') {
            step {
                echo 'Building the application'
            }
        }

        stage('Test') {
            step {
                echo 'Testing the application'
            }
        }

        stage('Deploy') {
            step {
                echo 'Deploying the application'
            }
        }
    }
}
